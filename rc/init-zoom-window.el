(use-package zoom-window
  :ensure t
  ;; :bind
  ;; (:prefix "C-c w"
  ;;  :prefix-map window-map
  ;;  :prefix-docstring "Window map."
  ;;  ("z" . zoom-window-zoom))
  :config
  (setq zoom-window-mode-line-color "#455d47")
  )




(provide 'init-zoom-window)
